[package]
name = "aperture-shared"
version = "0.1.0"
edition = "2021"
description = "Canonical cross-component encodings for Aperture: sealed order, attestation claim, settlement."

[dependencies]
odra = { version = "2.2.0", features = [], default-features = false }

[features]
default = []
